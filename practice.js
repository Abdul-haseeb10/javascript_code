// Function that calculates the sum of elements in an array
/*function calculateSum(array) {
    let sum = 0;
    for (let i = 0; i < array.length; i++) {
      sum += array[i];
    }
    return sum;
  }
  
  // Example array
  const numbers = [1, 2, 3, 4, 5];
  
  // Call the function with the array and output the result
  const result = calculateSum(numbers);
  console.log(`Sum of elements in the array: ${result}`);*/
 var myArray = [];
var i = 0;
while (i < 5) {
  myArray.push(i); // You need to push the value of `i` into the array
  i++;
}

console.log(myArray);

  